management:
  endpoints:
    web:
      exposure:
        include: refresh, bus-refresh
server:
  port: 8082

spring:
  application:
    name: second-client
  #  cloud:
  #    config:
  #      uri: http://localhost:8888
  profiles:
    active: development
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      profile: development
    bus:
      destination: myTopic
